# V-AEP Platform — codebase guide

Monorepo for the Vertical AI Employee Platform. Design spec: `docs/specs/2026-07-09-foundation-auth-design.md`.
The business context lives in the parent proposal (see `../CLAUDE.md`). Keep answers concise (token-minimizing setup).

## Stack
pnpm + Turborepo · `apps/web` (Next.js App Router, Tailwind, TanStack Query, Zustand, rhf+zod) ·
`apps/api` (NestJS, Prisma, Postgres) · `packages/types` (@vaep/types, shared DTOs) ·
`packages/config` · `infra/docker-compose.yml` (postgres+pgvector, redis, minio, adminer).

## Conventions (enforce these)
- **Singletons both sides.** Backend: `PrismaService` + all NestJS providers are singletons; tenant flows via a request guard. Frontend: one `apiClient`, one `queryClient`, one Zustand store.
- **Optimistic writes.** Mutations use TanStack Query `onMutate`→`onError` rollback→`onSettled` invalidate.
- **Minimal `useRef`** — only for focus, commented.
- Frontend `features/*` mirror backend `modules/*` one-to-one.

## Run locally
```
cd platform && pnpm install
docker compose -f infra/docker-compose.yml up -d              # infra
pnpm --filter @vaep/api run prisma:migrate                    # = prisma migrate deploy (apply committed migrations)
pnpm dev                                                      # web :3000, api :4000
```

GOTCHA (pgvector + Prisma): do NOT run `prisma migrate dev` to *apply* migrations. Prisma's schema can't
represent the HNSW index on the `Unsupported("vector")` column, so `migrate dev` sees it as drift and will
prompt to DROP `KnowledgeChunk_embedding_idx` (destructive). Use `prisma:migrate` (=`migrate deploy`) for
applying. To AUTHOR a new migration use `prisma:migrate:new` (=`migrate dev`), then before applying, delete any
`DROP INDEX ..._embedding_idx` line from the generated SQL (and keep the `CREATE ... USING hnsw` if the table
was recreated). If a `migrate dev` is Ctrl+C'd it can orphan the advisory lock (P1002 next run) — terminate the
idle backend holding `pg_advisory_lock` in Postgres, then retry.
GOTCHA (local port conflicts): a local Postgres holds `127.0.0.1:5432` and a local Redis holds `[::1]:6379`,
so Docker publishes Postgres on **5433** (`POSTGRES_PORT`) and Redis on **6380** (`REDIS_PORT`). The `.env`
files already use `localhost:5433` / `redis://127.0.0.1:6380`. Adminer :8080.

GOTCHA (monorepo build): `@vaep/types` is a built **CommonJS** package (`main`→`dist/index.js`; run `pnpm --filter @vaep/types build`, or `pnpm build`/turbo `^build` does it). `apps/api/tsconfig.build.json` sets `rootDir:"src"`, `paths:{}`, `incremental:false` so `nest build` emits `dist/main.js` (NOT `dist/apps/api/src/main.js`) and resolves `@vaep/types` from node_modules at runtime. Do NOT add source path-aliases (`../../packages/.../src`) to `tsconfig.build.json` — that hoists rootDir and scatters the entry. Typecheck (`tsconfig.json`) keeps source paths; jest maps `@vaep/types`→source.

## Deployment shape: `WORKFLOW_EXECUTION_MODE`
`queue` (default) enqueues a BullMQ job for the persistent `WorkflowProcessor`. `inline` executes the run
directly inside the request instead. **`inline` exists because a serverless-only deployment has no worker:
`QUEUE_WORKERS_ENABLED=false` removes the consumer but nothing stops the producer, so every run is created
and then sits `PENDING` for ever** (gap G40). `WorkflowEngine.execute/resume/trigger` are plain methods and
the processor is a thin wrapper, so inline needs no engine change — only the dispatch in `WorkflowsService`.
- Inline execution is **awaited**, so a run is often already terminal in the create response. Polling clients
  are unaffected; they just find it finished.
- **GOTCHA (real bug, caught by e2e):** in inline mode `activate()` must NOT register a BullMQ repeatable for
  a SCHEDULE trigger — the cron sweep already drives it, and if any worker is reachable the workflow fires
  **twice** per interval. `addSchedule`/`removeSchedule` no-op when inline.
- Time-based work that normally runs as a repeatable is exposed at **`/admin/cron/:job`**
  (`workflow-schedules`, `workflow-watchdog`, `approval-sla`, `hr-retention`), driven by Vercel Cron per
  `apps/api/vercel.json`. Auth is a shared secret: `X-Cron-Secret`, or `Authorization: Bearer` (what Vercel
  Cron sends). **Unset `CRON_SECRET` disables the routes** rather than leaving them open. Uses `@All` — Nest
  registers only one route if you stack `@Get`+`@Post`, and the other 404s silently on a schedule.
- Trade-offs to state out loud: no retry/durability, a wall-clock ceiling per run, one invocation held per
  running workflow, no queue-level rate limiting or DLQ. Exit ramp: deploy `main.ts` as one always-on worker
  with `QUEUE_WORKERS_ENABLED` unset and flip back to `queue` — no refactor.

## Provider knobs (swappable, self-hosted defaults)
- `EMBEDDINGS_PROVIDER`: `hash` (default, offline/deterministic — also used by tests) · `local` (transformers.js, lazy) · `openai` (lazy, needs `OPENAI_API_KEY`). All 384-dim.
- `STORAGE_PROVIDER`: `local` (default, `STORAGE_DIR`) · `s3` (MinIO/S3, lazy). Auth is behind `AUTH_PROVIDER` (JWT).
- `LLM_PROVIDER`: `mock` (default, deterministic/offline — used by tests) · `openai` (needs `OPENAI_API_KEY`) · `anthropic` (needs `ANTHROPIC_API_KEY`). Model always from `LLM_MODEL`; defaults are `gpt-5.6-terra` / `claude-sonnet-5`. **Never hardcode a model in calling code** — a deprecation must be a config change. Both providers are at deliberate feature parity (streaming, native tool-result threading, configurable `maxTokens`, abort) so switching really is just this one env var; if one grows a capability the other lacks, it belongs in `LlmProvider` as an optional method, not a caller-visible special case. `@anthropic-ai/sdk` and `openai` are both real dependencies now (the Anthropic one used to be lazily imported but absent, so `LLM_PROVIDER=anthropic` failed at first call, not at boot).

## Module status (one module per turn: backend module + mirrored frontend feature, verify, update memory)
- ✅ Foundation + **auth/tenant**: register/login/refresh/me, JWT, multi-tenant `Company`/`User`.
- ✅ **Knowledge/RAG**: upload → BullMQ ingest (extract/chunk/embed) → pgvector(384, HNSW) tenant-scoped cosine `/search`. Frontend: optimistic upload, polling doc list, search panel, `/knowledge` page (kept, no longer in the main Sidebar nav — see role-scoping below).
  - ✅ Role-scoped by category: `KnowledgeDocument`/`KnowledgeChunk.category` (nullable `EmployeeRole`, `null` = Shared/company-wide) filters both `/knowledge` list/search and AI-employee chat retrieval (`RetrievalService` threads `employee.role`); `PATCH /knowledge/documents/:id/category` retags (transactional, cascades to chunks). The workflow `RETRIEVE` node stays intentionally unscoped (company-wide). Frontend: retag dropdown on every document row, per-employee "Knowledge" tab (`employees/[id]`) filtered to that role + Shared, defaults new uploads there to the employee's own role.
- ✅ **AI Employee runtime**: `AiEmployee` (roles, status pause/disable), Conversation/Message (memory), `EmployeeMemory`. `AgentRuntimeService` = plan→retrieve(reuses KnowledgeService)→memory→act(bounded tool-calling loop, max 3)→validate(citations+confidence+approval). Swappable `LlmProvider`. Frontend: `/employees` list+create+status, `/employees/[id]` chat with sources/plan/validation/tool-calls. Paused/disabled → 409.
- ✅ **Skills**: code-defined catalog (slack/email/stripe/github/http, each w/ tools) → `InstalledSkill` (company) → `EmployeeSkill` (assigned) → runtime tool-calling; every action logged in `SkillExecution` (audit). Mock/sandbox executors (offline, deterministic; real executors + creds encryption = TODO). Frontend `/skills` (catalog+installed) + employee skill picker + chat "actions taken" panel.
  - ✅ Per-employee skill connections are live: the same skill (e.g. gmail) can be installed more than once per company — company-wide (`employeeId` null) or owned by one specific AI Employee (unique on `companyId, skillKey, employeeId`); execution-time resolution prefers the acting employee's own connection over the company-wide one. A workflow's EVENT trigger can scope itself to one connector (`triggerConfig.connectorId`) so `fireEvent` only fires the workflow tied to the firing connector (unscoped triggers keep matching every connector, back-compat) — proven end-to-end with two Gmail connections + two workflows in `per-employee-skill-connections.e2e-spec.ts`. Frontend: employee-page "connect just for me" skill install + connector-scoping dropdown on the EVENT trigger editor.
- ✅ **Workflow builder**: `Workflow` (graph JSON nodes+edges), `WorkflowRun`+`WorkflowStepRun` (per-node audit). `WorkflowEngine` on a BullMQ `workflow-run` queue walks the graph threading a context; nodes TRIGGER/RETRIEVE(Knowledge)/AI_STEP(shared LlmProvider)/TOOL_ACTION(Skills)/WAIT/CONDITION/NOTIFY; `{{a.b.c}}` template resolver (no eval). Frontend `/workflows` list + linear step builder + run log (polling). WAIT is a bounded sleep (durable resume = TODO); triggers are manual (scheduled/webhook = TODO).
  - ✅ AI-assisted drafting live: `POST /workflows/generate` (BUSINESS/ENTERPRISE-gated via `PlanGuard`/`@RequirePlan`) grounds a chat-style prompt in the company's real installed skills + hired employees, self-corrects once, and returns a `draft`/`question` result — never persists; hand the returned `definition` to the existing `POST /workflows` to create it.
- ✅ **Onboarding (Steps 2–5)**: rich Company profile (industry/size/country/timezone/website/logoUrl/description) + `onboardedAt`, `User.phone`, rich `AiEmployee` config (department/manager/workingHours/timezone/language/`knowledgeAccess`/budgetLimit/`permissions`/`approvalRules`). Backend: register persists profile+phone; `PATCH /companies/current`; onboarding module (`/onboarding/status|catalog|complete` → hires employees, stamps `onboardedAt`); `knowledgeAccess=NONE` skips retrieval. Frontend: expanded register form, post-auth routing by `onboardedAt`, 3-step AI onboarding wizard, Employee Settings panel. Company = tenant (org/workspace conceptual; no separate tables). `permissions`/`approvalRules`/`budgetLimit` persisted, enforcement = Approval Center TODO.
- ✅ **Approval Center (Step 11)**: `ApprovalRequest` (PENDING/APPROVED/REJECTED). `ToolExecutorService` intercepts high-risk tool calls (catalog `highRisk` e.g. stripe, or employee `approvalRules` requireApprovalForAllTools/requireApprovalForTools) → creates a PENDING request instead of executing; runtime records `pendingApproval` + stops the loop. Manager Approve (execute now, logs SkillExecution) / Reject / Modify (edit args + execute). Frontend `/approvals` queue (optimistic) + nav badge + chat awaiting-approval badge. DI: Skills←Approvals←Employees (no cycle).
- ✅ **Skill Config & Connection (Steps 4–5)**: `InstalledSkill` connectionType/connectionStatus/credentials (secrets masked → `credentialsSet`); catalog `connection` + data-driven `configSchema` (+ gmail/hubspot/jira/calendar/gdrive via generic mock); `PATCH .../config`, `POST .../connect|disconnect`. Execution stays connection-agnostic for mock (real OAuth + creds encryption + CONNECTED-gating = TODO). Data-driven config form frontend.
- ✅ **Analytics/KPI dashboard (Step 12)**: `/analytics` overview|employees|activity (aggregation, no migration); ops `/dashboard` (stat tiles + range + per-employee + activity).
- ✅ **Billing (Steps 1,13)**: `Subscription` + PLAN_CATALOG + usage; default STARTER on register (self-heal on GET); swappable BillingProvider (mock default / stripe lazy); `/billing`.
- ✅ **Marketplace (Step 14)**: code catalog — install AI-employee/workflow templates (delegate to create) + skills reuse; `/marketplace`. No migration.
- ✅ **Workflow triggers (Steps 8,9,11)**: `triggerType` MANUAL/SCHEDULE(BullMQ repeatable)/WEBHOOK(public token route, no JWT)/EVENT; activate/deactivate; `fireEvent`; processor handles `{runId}` and `{workflowId,source}`.
- ✅ **Continuous Learning (Step 15)**: `EmployeeFeedback` (👍/👎 + correction) → FACT `EmployeeMemory` (source FEEDBACK) the runtime already recalls; memory curation (teach/forget) + `/employees/:id/learning` summary.
- ✅ **ALL canonical Steps 1–15 modules covered (13 e2e suites, 76 tests).** Remaining are enhancements, NOT modules: real OAuth flows + creds encryption; Stripe hosted-checkout + webhooks; token/voice usage metering; hard plan-limit enforcement; SSO; semantic memory recall (embeddings; FACTs can be crowded past RECENT_MEMORY_LIMIT); internal event emitters wired to real platform events; charts/trend snapshots; third-party publisher marketplace + commission; company logo upload.
- **P0/P1 gap-remediation COMPLETE**: ✅ RBAC+Users (`modules/users`, RolesGuard/@Roles, `/team`) · ✅ creds encryption (`common/crypto`, AES-GCM) · ✅ real executors+OAuth+Stripe webhooks (`SKILL_EXECUTOR`/`BILLING_PROVIDER`; mock=tested default, real needs live keys) · ✅ workflow APPROVAL node (WAITING→resume) · ✅ employee goals+KPI targets (analytics attainment) · ✅ security-policy/team/department (`modules/organization`, `/organization`). Company=org=workspace (decided, no split). Real-integration env: `SKILL_EXECUTOR=real|auto`+`OAUTH_*`; `BILLING_PROVIDER=stripe`+`STRIPE_*` (+`pnpm add stripe`); `ENCRYPTION_KEY` in prod.
- **Architecture [TARGET] implemented** (per `docs/architecture/connector-event-workflow-architecture.md`): ✅ **Event ingestion pipeline** (`modules/events`: signed `POST /connectors/:id/webhook` → RawEvent → BullMQ `event-normalize` → CanonicalEvent → `fireEvent`; GitHub+generic drivers/mappers; `GET /connectors/:id/events`, `/events/canonical`) · ✅ **Connector health** (DEGRADED/DISCONNECTED, `ConnectorHealthService`, single-flight token refresh, scheduled probe+reconcile skeletons, `/connectors/:id/health`) · ✅ **Resilience** (`common/resilience`: Redis circuit breaker on egress, retry classifier, per-connector rate limiter, `RESILIENT_JOB_OPTIONS`+DLQ, `/admin/dlq` + `/admin/health`) · ✅ **EVENT condition DSL** (`triggerConfig.conditions[]`) + **event→run correlation/lineage** (`WorkflowRun.triggerEventId/correlationId`, `GET /events/canonical/:id/lineage`). Real per-provider drivers (Gmail/Graph/Salesforce/Stripe-business), Kafka backbone, full OTel span propagation remain [TARGET].
- ✅ **HR staff records (Wave P3-01)** (`modules/hr`, `/hr/*`): 6 models `StaffMember`+satellites `LeaveRequest`/`StaffDocument`/`PerformanceReview`/`OnboardingTask`/`AttendanceRecord` (migration `20260801210000_p3_01_hr_domain`). **Whole domain is OWNER/ADMIN-only, reads included** (special-category PII — stricter than the org module's "members can read"). Special-category/personal PII (`LeaveRequest.reason`, `PerformanceReview.aiDraft`/`finalReview`, `StaffMember.personalEmail`/`phone`, `StaffDocument.fileName`) is **encrypted at rest** via the global `CryptoService` (`v1:` envelope in the same String column; seal-on-write/open-on-read in `hr-pii.util.ts`; e2e asserts ciphertext in DB, plaintext via API). Daily retention sweep (`HrRetentionService` + BullMQ repeatable, worker-gated) honours `SecurityPolicy.dataRetentionDays` — prunes **satellite records only** (never the `StaffMember` roster), `0`=keep-forever; `POST /hr/admin/retention/run-now`. **Deviations from plan (documented):** authoritative DDL was `docs/architecture/database/2026-08-01-complete-database-design.md` §HR (the frozen `proposed-prisma-changes.prisma` had no HR DDL); "passports encrypted" = document files live in object storage (`storageKey`), there is **no** passport/ID-number DB column; only `StaffMember` keeps a `company` relation, the 5 satellites carry plain `companyId`+index (§5.7). Legal-hold is not modeled yet (retention is purely time-based).
- ✅ **Workflow templates (Wave P3-02)** (`modules/workflow-templates`, `/workflow-templates/*`): new `WorkflowTemplate` model (migration `20260801220000_p3_02_workflow_templates`; also adds `Workflow.sourceTemplateId`/`sourceTemplateVersion` provenance + `installIdempotencyKey` partial-unique). `POST /:id/install` performs a **deep COPY** (params substituted via the existing `{{param.*}}` resolver) into the tenant → **DRAFT workflow + v1 PUBLISHED version** in one transaction, provenance recorded, **never auto-activated**; `Idempotency-Key` header dedups (returns the original). Prereq check (installed skills / hired employee roles / min plan) → **422** with the exact list. First-party catalog (`workflow-templates.catalog.ts`, `companyId=null`) is validated + **seeded on boot** (`onModuleInit`, upsert by key+version). `POST /workflow-templates` authors a tenant template through the SAME publish validation (`validateManifest` → `validateDefinitionStructure`, which already rejects `DB_QUERY` + inline secrets). Browse routes are any-member; install/create are OWNER/ADMIN. **Clean leaf module** — reuses only PURE functions (`validateDefinitionStructure`, `resolveTemplate`, `toWorkflowDto`, `SkillCatalog`) + global Prisma/Audit, so no cross-module import/cycle. **Note:** coexists with the older marketplace code-catalog (`marketplace.catalog.ts` `WORKFLOW_TEMPLATES`, delegate-to-create) — the DB-backed model is the doc-19 path; the marketplace shim is untouched. **Hardening (migration `20260801230000_p3_02b_template_owner_unique`):** template keys are unique **per-owner** (`@@unique([companyId, key, version])`, NOT global) so tenant B authoring a key tenant A used doesn't collide — first-party rows (companyId=null) aren't DB-deduped by that (NULLs are distinct), so the boot seeder upserts them by hand (findFirst→update/create). A `binds:'employee'`/`binds:'skill'` install param is validated against real tenant resources at install (→422), not left to fail at runtime.
- ✅ **HR + Marketing workflow templates (Waves P3-03 + P3-04)**: **22 first-party templates** — 11 HR (`hr-workflow-templates.catalog.ts`) + 11 Marketing (`marketing-workflow-templates.catalog.ts`), aggregated by `workflow-templates.catalog.ts` → seeded on boot. Reverse-engineered from docs 27/28, **frozen-17 vocab only** (AI_EMPLOYEE_STEP + TOOL_ACTION; the legacy AI_STEP/NOTIFY are banned by doc 27 §0.4). Every mandated APPROVAL is a single-forward gate (reject fails the run) and **never inside a LOOP**; social scheduling/publishing rely on the postiz `schedule_post`/`publish_now` **highRisk auto-gate** instead of an explicit APPROVAL; `mkt.social-publish` checks `get_post_status` BEFORE `publish_now` (double-post-safe). Params bind to tenant resources (`binds:'employee'`/`'channel'`). Guarded by `workflow-templates.catalog.spec.ts` (runs the boot-seed `validateManifest` on all 22 — fast, no DB) so a broken template fails a unit test, not app startup.
- ✅ **Approval routing + SLA + canDecide / R12 (Wave P3-05, complete)** (migration `20260801240000_p3_05_approval_routing`): **🔴 SECURITY — the `@Roles('OWNER','ADMIN')` guard on `POST /approvals/:id/{approve,reject,modify}` was REMOVED** and replaced by a service-level `ApprovalRoutingService.canDecide` (doc 08 §8.1.6, ledger R12 / doc 00 G30 — adopted per the "prefer the canonical docs" decision). An **unrouted** request still requires OWNER/ADMIN (the mandatory §8.1.11 regression, tested at unit + HTTP level); a **routed** request is decidable only by whoever the rule names (USER/ROLE/DEPARTMENT/TEAM/EMPLOYEE_MANAGER/ANY_ADMIN). New **cycle-safe** `modules/approval-routing` (PrismaService-only; imported by BOTH Workflows-engine and Approvals, like the LlmModule fork) resolves rules → assignees. `ApprovalRequest` gained routing/chain columns (chainId/level/escalationTier/assignee/rule/dueAt/routingSnapshot…) + `User.departmentId/teamId/managerUserId` + `AiEmployee.managerUserId` + `SecurityPolicy.defaultApprovalSlaMinutes`. **Multi-level chains** = N rows sharing `chainId`, next level created lazily on approve (run stays WAITING until the final level). `GET /approvals/:id/history` returns the whole chain; `GET /approvals?assignedToMe=true` = the "my inbox" view. **§8.2 SLA sweep** (`modules/approvals/sla/*`, `approval-sla` queue + worker, 5-min repeatable mirroring the workflow watchdog): a cross-tenant sweep (`[status,dueAt]` index) on breached `dueAt` → escalate through the level's `escalationChain` (`ESCALATED` + new tier row via `escalatedToId`), else apply `onTimeout` (`AUTO_APPROVE`/`AUTO_REJECT` via the SAME resumeRun/cancelRun/runTool effect paths with `autoDecided:true`, or `EXPIRED` + cancelRun). Every transition is a race-safe guarded `updateMany WHERE status='PENDING'`. `onTimeout` defaults to `NONE` (never AUTO_APPROVE) — §8.2.11. **GOTCHA:** a `USER`-routed approval is decidable ONLY by that user (canDecide is rule-specific, no OWNER override — doc 08 §8.1.7); the doc-sanctioned answer to "that user is unavailable" is an SLA `escalationChain` reaching `ANY_ADMIN`, NOT an admin override. Tests: `approval-routing.spec.ts` (11 unit), `approval-routing.e2e-spec.ts` (4, incl. TOOL-kind), `approval-sla.e2e-spec.ts` (4: escalate/auto-approve/expire/race).
- ✅ **Workflow permissions (Wave P3-06 — completes P3)** (`modules/workflow-permissions`, `/workflows/:id/permissions`, migration `20260801250000_p3_06_workflow_permissions`): `WorkflowPermission` model per **doc 09 §9.C.5** (chosen canonical over the superseded proposed-prisma) — a grant ties a subject (`USER`/`ROLE`/`DEPARTMENT`/`TEAM`/`EMPLOYEE`) to an action (`VIEW`/`EDIT_GRAPH`/`UPDATE`/`PUBLISH`/`RUN`/`DELETE`/`MANAGE_PERMISSIONS`) on one workflow. **`workflow:run` is enforced at ENQUEUE** (`WorkflowsService.enqueueRun`, doc 16 §21 — NOT per-attempt): a workflow with **no** RUN grants is runnable by any member (back-compat); adding RUN grants restricts it to matching subjects (+ OWNER/ADMIN bypass). Run-as **subject** = the clicking user (MANUAL) or the pinned version's `publishedById` with role re-resolved fresh (SCHEDULE/EVENT/WEBHOOK, §9.C.3). **Company kill-switch (§9.C.5):** the run-subject query filters `status:'ACTIVE'`, so a later-DISABLED publisher stops authorising a restricted workflow's automated runs (their permissions don't live on). CRUD (`GET/POST/DELETE`) gated by **owner-or-admin** (`Workflow.ownerUserId` = creator, so an owner can share without being a company admin — §9.C.6), NOT a blanket `@Roles`. Cycle-safe leaf module (Prisma-only) imported by WorkflowsModule. **SCOPE:** this is the workflow-level RUN layer only; doc 09's full per-attempt node-permission PDP (role capabilities, `node.requiredPermission`, `RoleScopeAssignment`, execution-time skill enforcement) is **deferred** (the plan + doc 16 §21 scope P3-06 to enqueue-time). Tests: `workflow-permissions.spec.ts` (7 unit), `workflow-permissions.e2e-spec.ts` (6, incl. USER + DEPARTMENT scoping + manage-gating + tenant isolation). **P3 wave (HR/Marketing integration) is now COMPLETE (P3-01…P3-06).**
- ✅ **Workflow Builder frontend spec + backend gap-closure (2026-08-02)**: the full builder UX spec is `docs/architecture/workflow-system/29-workflow-builder-frontend-spec.md` (AI-Employee-OS, not n8n). Backend gaps it surfaced were closed: **`GET /workflows/node-definitions`** → `NodeDefinitionDto[]` (static `engine/nodes/node-catalog.ts`, category/handles/configSchema/`hasSideEffects`/`canPauseForApproval` for all 19 node types; boot-guard `node-catalog.spec.ts`; kills the client node-registry) · DTO fields added — `WorkflowDto`.{ownerUserId,activeVersionId,draftVersionId,category}, `WorkflowRunDto`.{failureClass,resumeNodeId,startedByUserId,workflowVersionId}, `WorkflowStepRunDto`.attempt, `UserDto`.{departmentId,teamId,managerUserId} (all mappers + response-schemas synced) · run controls **`POST /workflows/runs/:id/{cancel,retry}`** · **EVENT single-active ENFORCED** at activate (same eventType + overlapping connector → 409, `assertNoConflictingEventTrigger`). Tests: `workflow-run-controls.e2e-spec.ts` (5) + `node-catalog.spec.ts` (9). **Deferred (documented in doc 29):** realtime WS gateway P5-01 (seq-outbox exists but state-machine-only + off by default; 1s poll is the path), `/runs/:id/{timeline,attempts,tool-calls}` + compensate (P5-02/03), version diff/rollback/clone (client composes), live Slack channel picker (free-text degrade), AiEmployee.avatarUrl (initials).
- Deferred (not started): token/voice metering, SSO, semantic memory recall, analytics charts, publisher-marketplace+commission, logo upload, email invites, audit log, enforce mfa/session/retention, AiEmployee.departmentId FK, bulkheads, DLQ auto-replay/alerting, watch-subscription renewal.
- Run e2e: from `apps/api`, `pnpm test` with `SKILL_EXECUTOR=mock BILLING_PROVIDER=mock ENCRYPTION_KEY=<64hex> LLM_PROVIDER=mock EMBEDDINGS_PROVIDER=hash STORAGE_PROVIDER=local` + `DATABASE_URL`+`REDIS_URL`+JWT secrets (currently **249 e2e tests, 39 suites** + **277 unit, 33 suites** via `pnpm run test:unit`; jest-e2e runs serial `maxWorkers:1`). **The suite is 100% green — there are NO known-failing tests. If one fails, it is a real regression; do not dismiss it as environmental.** (2026-08-01, P0-02: the five previously "known flaky" suites were env-dependent, not flaky. `test/setup-e2e-env.ts` now blanks every OAuth credential before ConfigModule loads — that had to move out of `beforeAll` because `ConfigModule.forRoot()` runs at *import* time — and `e2e/engines-support.e2e-spec.ts` now forces the AUTO executor via `overrideProvider` instead of silently depending on `.env`'s `SKILL_EXECUTOR=auto`, which would have failed under CI's `mock`.) Lint: `pnpm -w run lint` (ESLint 9 flat config at the repo root; type-aware async rules on `apps/api` only). GOTCHA: `jest-e2e.json` sets `testTimeout: 30000` — Jest's 5s default is not enough for a Nest app boot under load, and it surfaced as a confusing intermittent "Test suite failed to run" on a DIFFERENT suite each run while every test passed.
