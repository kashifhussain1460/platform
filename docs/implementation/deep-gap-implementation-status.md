# Deep Gap Implementation — Status

Tracks execution against `docs/implementation/deep-gap-implementation-baseline.md`. Updated after every phase, per
the governing instructions — never marked DONE without evidence (code + passing test + no bypass path).

**Environment note (affects every entry below):** full e2e verification (real Postgres/Redis, `describeIfDb` suites)
was not run this pass — Redis showed 379 connected clients vs. this repo's documented idle baseline of ~2, a
pre-existing environment-pollution issue (unrelated to this work, likely orphaned processes from a concurrent
session). Per explicit user direction, verification for this phase relied on: full unit suite, `tsc --noEmit`,
and `eslint` on every changed file. E2E re-verification is a stated follow-up, not silently skipped.

**Concurrent work note:** a separate, parallel session was implementing a `CreditsModule`/`ModelCostRate` credit
system in this same repository during this phase (files: `modules/credits/*`, `modules/admin/{admin.module,
cron.controller}.ts`, `employees/{employees.service,conversations.controller}.ts`, `knowledge/embeddings/*`,
`workflow-runtime/traversal.service.ts`, `test/journey-hr-e2e.e2e-spec.ts`, migration
`20260819000000_credits_foundation`). None of it overlaps with the files this phase touched — confirmed via
`git diff --stat` before and after. It is flagged here because it directly affects the P1 gap **C-09** (see below).

---

## Phase 1 — Support Safety P0s + S-02 (COMPLETE)

| Gap | Previous State | Current State | Evidence | Tests | Remaining Risk |
|-----|----------------|---------------|----------|-------|----------------|
| S-02 | `chatwoot.resolve_conversation` updated the local mirror and returned `ok:true` — a silent-success defect; the real Chatwoot ticket never changed | Returns an honest `ok:false` / "NOT YET IMPLEMENTED" result; does **not** touch the DB | `real-skill-executor.ts` (`chatwootResolveConversation`) | `real-skill-executor.spec.ts` — updated case asserts `ok:false` + `supportConversation.update` never called | Real Chatwoot resolve call itself is still not implemented (unchanged scope decision — needs source-grounding or a live instance, tracked, not silently dropped) |
| S-04 | `chatwoot.reply_to_conversation`/`resolve_conversation` had no `highRisk` flag; an EVENT-triggered workflow could send a real reply with zero approval gate | Both tools flagged `highRisk:true` in the catalog — the existing shared `toolRequiresApproval` mechanism now gates both the chat path and the workflow TOOL_ACTION path identically | `catalog.ts` | `tool-approval-policy.spec.ts` (unit); new e2e `engines-support.e2e-spec.ts` case: a no-APPROVAL-node workflow calling `chatwoot.reply_to_conversation` pauses `WAITING`, zero `SkillExecution` rows until approved | None identified — closes the gap by reusing the exact mechanism already proven for Postiz/Stripe |
| S-01 | `ValidationService`'s `needsApproval` (confidence/grounding) was computed but only ever reached `WorkflowStepRun.output` — never gated a later TOOL_ACTION | Engine writes a reserved `__validation:<nodeId>` context key when a node's output carries `needsApproval:true`; both approval gates (`workflow-engine.service.ts` legacy, `approval-gate.service.ts` durable) now also check `contextHasUnresolvedValidationConcern(context)`, guarded by `hasAnyGrantedApprovalThisRun` so an already-approved run's LATER, unrelated, non-highRisk step isn't double-gated | `tool-approval-policy.ts`, `workflow-engine.service.ts`, `approval-gate.service.ts`, `node-attempt.processor.ts` | `tool-approval-policy.spec.ts` (unit, the predicate); new e2e case in `engines-support.e2e-spec.ts`: a low-confidence AI_EMPLOYEE_STEP draft forces approval on a downstream **non-highRisk** `slack.send_message` step, then completes once approved | The double-gate regression risk (caught during implementation, not after) is fixed by `hasAnyGrantedApprovalThisRun` — re-verified against `journey-hr-e2e.e2e-spec.ts`'s existing single-approval-then-notify shape by code inspection; full e2e re-run still pending (see environment note) |
| S-06 | Zero sensitive-scenario handling anywhere (refund/legal/PII/identity/account-deletion/security-incident/high-risk-sentiment/human-request) | New `SensitiveScenarioService` (shared, in `employees/runtime/`, not Support-specific): 7 action-oriented keyword-pattern categories + 1 scored sentiment heuristic (deliberately not a flat keyword list). Wired into `AgentRuntimeService.completeTurn` — a detected signal forces `needsApproval:true` and is recorded on `MessageValidationDto.sensitiveScenario`, feeding the SAME S-01 gate mechanism | `sensitive-scenario.service.ts`, `agent-runtime.service.ts`, `packages/types/src/index.ts` (new `SensitiveScenarioCategory`/`SensitiveScenarioSignal`) | `sensitive-scenario.service.spec.ts` — 10 cases incl. 2 explicit false-positive guards (a benign "what is your refund policy" question and a routine "credit card" payment question must NOT trip it — both are real strings already present in this repo's other e2e fixtures, checked deliberately) | A genuine LLM-based semantic layer (catching paraphrases neither pattern nor sentiment scoring would match) is explicitly deferred — documented in the service's own doc comment as a stated limitation, not a silent one, since it would be inert under the deterministic mock provider this repo tests against |
| S-13/C-06 | No human handoff mechanism existed at all; `AiEmployee.status` is company-wide only, no per-conversation pause | New `HandoffModule`/`HandoffService` (`escalate`/`resolve`), reusing `ApprovalRoutingService.resolveStep`/`canDecide` in full — no second routing resolver. New `SupportConversationStatus.ESCALATED` + `HandoffRequest` model (migration `20260819010000_s13_c06_handoff`). Defense-in-depth guard added directly in `real-skill-executor.ts`'s `chatwootReplyToConversation`: an `ESCALATED` conversation refuses a reply regardless of how the call was triggered | `handoff.module.ts`, `handoff.service.ts`, `handoff.controller.ts`, `schema.prisma`, `real-skill-executor.ts` | `handoff.service.spec.ts` — 7 cases (escalate, idempotent re-escalate, not-found, resolve resume=true/false, unauthorized resolve rejected, no-op on already-resolved); `real-skill-executor.spec.ts` — new case proving the ESCALATED guard fires before any Chatwoot/account lookup | **Scoped deliberately, documented, not silently dropped:** `SensitiveScenarioService`'s detection does NOT yet auto-call `HandoffService.escalate()` — the generic chat runtime (`AgentRuntimeService`) has no reliable link to which `SupportConversation` (a materially different id space) a given chat turn corresponds to; that mapping only exists inside a downstream `TOOL_ACTION`'s templated `conversationId` arg. Auto-wiring this requires threading the SupportConversation id through workflow context to the chat runtime, which doesn't exist today. Today, Handoff is a complete, tested, callable capability (via `POST /support/conversations/:id/escalate`, or a future `chatwoot.request_handoff` tool) — not yet automatically triggered by S-06. Tracked as a P2 follow-up, not silently claimed done. |

**Full unit suite:** 83 suites / 740 tests passing (`pnpm run test:unit` equivalent, `--maxWorkers=2` locally to avoid an
unrelated OOM at default concurrency — not a code issue). `tsc --noEmit` clean. `eslint` clean on every changed file.

**Migration applied:** `20260819010000_s13_c06_handoff` — hand-authored (this environment's non-interactive shell
blocks `prisma migrate dev`) from a reviewed `prisma migrate diff` output, with two lines deliberately excluded:
the documented pgvector/HNSW `DROP INDEX "KnowledgeChunk_embedding_idx"` false-drift, and an unrelated
`RenameIndex` belonging to the concurrent session's in-progress `EmployeeCreditPeriodCounter` work. Applied via
`prisma migrate deploy` (54 migrations now applied, up from 53). Additive only — new enum value, new table, new
FKs — no existing table altered, no data at risk.

**Kill-critic check for this phase (per the governing instructions):**
1. Duplicate service created? No — `HandoffService` reuses `ApprovalRoutingService` in full; `NotificationsService`
   gained one new method (`handoffRequested`) reusing the existing `approvalRecipients` resolver, not a new one.
2. Provider-specific logic in Core? No — `SensitiveScenarioService`/`tool-approval-policy.ts` additions are
   engine-agnostic; the ESCALATED guard is the one genuinely Chatwoot-specific line, correctly left in the engine's
   own executor, not lifted into shared code.
3. Can the external action bypass approval? Checked directly — `chatwootClient.sendReply` has exactly one caller
   in production code (`real-skill-executor.ts`), and the approval gate runs upstream of executor dispatch in both
   the chat path and both workflow engines.
4. Can it execute twice? Not addressed this phase (that's M-06, still pending).
5. Can a worker crash orphan an external effect? Not addressed this phase (M-06/C-07).
6. Cross-tenant access? All new Handoff queries are `companyId`-scoped; unit-tested via the "wrong company" NotFound case.
7. Can an LLM claim success without provider confirmation? Directly fixed by S-02 for `resolve_conversation`.
8. Expensive-loop trigger? Not applicable to this phase's changes.
9. Sensitive data retained indefinitely? Not addressed this phase (S-09, still pending).
10. Reusable by a future AI Employee? Yes by design — `SensitiveScenarioService`/`HandoffService` are role-agnostic;
    `HandoffRequest` is currently FK'd to `SupportConversation` specifically (documented scope decision above), so a
    future non-Support handoff use case would need its own conversation-linkage design, not a blocker for HR/Sales
    reuse of the *service* itself once that link exists.
11. Weakened an existing safety mechanism? No — S-01's guard against double-gating a later step was found and fixed
    during implementation (not after), verified against `journey-hr-e2e.e2e-spec.ts`'s existing approve-once-then-
    notify shape by direct code inspection.
12. Works in both execution modes? Both `workflow-engine.service.ts` (legacy) and `node-attempt.processor.ts`/
    `approval-gate.service.ts` (durable) were changed together for S-01; full dual-mode e2e re-run is the pending
    verification step (see environment note).
13. Is the test actually executed? Yes — every number above is from an actual `jest` run this session, not assumed.
14. Is the deployed code the code verified? Not applicable yet — nothing from this phase has been proposed for
    deployment; `deployment` branch promotion remains the separate, unaddressed governance item (C-15) from the baseline.

**Not yet done from the baseline's Phase 2+ (§7 items 5-11), unstarted:** C-07 (resilience wiring, absorbs M-07/C-10),
M-06 (idempotency), S-07 (TOCTOU + migration), C-09 (**needs re-scoping** — the concurrent session's new
`CreditsModule` may already supersede or need to compose with this gap's original design; do not build a second
mechanism), S-09 (retention), M-08 (consent wiring), M-10 (analytics).

---

## Phase 2 — External Action Safety: C-07, M-06, S-07 (COMPLETE)

| Gap | Previous State | Current State | Evidence | Tests | Remaining Risk |
|-----|----------------|---------------|----------|-------|----------------|
| C-07 | `PostizClientService`/`ChatwootClientService` made raw, unwrapped `fetch()` calls — no circuit breaker, no rate limiter on either the tool-call path OR `MarketingSyncService.sweep()`'s direct calls (which bypassed `SkillsService` entirely) | New `ResilientClientBase` (`common/resilience/`) — every outbound call now goes through `guardedFetch`. Postiz uses ONE global resource key + a 90/hr rate budget (single shared instance, per M-07/C-10's own finding — absorbed here, not built as a second mechanism). Chatwoot uses ONE key PER `chatwootAccountId` (per-tenant instance — one tenant's outage must not trip the breaker for another tenant), no rate limit (none documented for Chatwoot). `SkillsService.runTool` gained a `SELF_WRAPPED_SKILL_KEYS` gate so its own generic, `installedSkillId`-keyed wrap doesn't ALSO apply (wrong identity, double-wrap) | `resilient-client.base.ts`, `postiz-client.service.ts`, `chatwoot-client.service.ts`, `marketing.constants.ts`, `skills.service.ts` | `resilient-client.base.spec.ts` (5 cases: pass-through, breaker trips + fast-fails without calling fetch again, per-key isolation, shared-budget-across-two-callers = the direct M-07/C-10 proof, no-rate-limit-when-none-supplied); `postiz-client.service.spec.ts` (new case proving `listPosts()` — the exact method the sweep calls directly — is protected by the same breaker) | If Postiz ever gains per-company (`Customer`/group) isolation (M-12, still open, out of scope), the global resource-key choice should be revisited — correct for the system as it exists today, not a permanent assumption (stated in the code comment) |
| M-06 | `postiz.schedule_post` had NO idempotency at all (unlike its sibling `publish_now`, which has its own working `ScheduledPost.idempotencyKey` pattern, left untouched); a retried call created a real duplicate scheduled post at Postiz with zero local trace | New generic, engine-agnostic `ToolIdempotencyService` (`common/idempotency/`, new `ToolIdempotencyRecord` model, migration `20260819020000_m06_tool_idempotency`) — record-before-effect, replays a `COMPLETED` result within a window, rejects a still-`PENDING` concurrent attempt, allows a fresh attempt after `FAILED` or an expired window. Applied to `schedule_post` (24h window, matching `publish_now`'s) AND `chatwoot.reply_to_conversation` (5-min window — short, because a support agent may legitimately send an identical short reply hours apart) | `real-skill-executor.ts` (both tool methods rewritten), `RealSkillExecutor` gained a 9th constructor param (mechanically updated across `skills.module.ts`'s factory + 2 e2e spec files + the unit spec's 26 call sites) | `tool-idempotency.service.spec.ts` (7 cases covering every branch: first call, replay-within-window, in-flight-PENDING-rejection, fresh-attempt-after-FAILED, fresh-attempt-after-window-expiry, effect-throws→FAILED+rethrow, non-P2002 error passthrough); new `real-skill-executor.spec.ts` case proving a retried `schedule_post` call hits Postiz exactly once | None identified for the mechanism itself. A latent, PRE-EXISTING, unrelated bug was found and fixed opportunistically while editing this exact code: `publishIdempotencyKey`'s separator was a literal raw NUL byte in the source file (not the `\0` escape the comment describes) — same runtime behavior either way, but it made the file register as "binary" to grep/ripgrep; fixed to the correct escape sequence, zero behavior change |
| S-07 | `ChatwootAccount.chatwootAccountId` had no `@@unique` (it is the webhook's own auth-lookup key — a duplicate is a cross-tenant risk, not just data-quality); `SupportConversation(companyId, chatwootConversationId)` was only `@@index`, not `@@unique` — a real TOCTOU race in the webhook's findFirst-then-create | Both fields now `@@unique` (migration `20260819030000_s07_support_unique_constraints`). Controller: `chatwootAccount.findFirst`→`findUnique`; the conversation's findFirst-then-create/update replaced with one atomic `upsert` (`companyId_chatwootConversationId` compound key) — Postgres resolves the race via `INSERT ... ON CONFLICT DO UPDATE`, not application code | `support-webhook.controller.ts` | `support-webhook.controller.spec.ts` (9 cases, all updated to the new `findUnique`/`upsert` shape; proves the correct atomic primitive is called with the right args) | **Pre-migration remediation, done and disclosed:** the dev DB had 28 `ChatwootAccount` rows all sharing `chatwootAccountId='1'` — confirmed via company names ("Support Test Co"/"Support Engine Test Co") to be `engines-support.e2e-spec.ts` fixture debris from repeated test runs, not real data. **User-approved** `UPDATE "ChatwootAccount" SET "chatwootAccountId" = id;` de-duplicated them (no data loss) before the migration was applied — documented in the migration file's own header comment. **Genuine concurrent-request proof (two simultaneous webhook deliveries actually racing) requires a real-DB e2e test, not a mocked unit test** — the unit tests prove the controller now calls the correct atomic primitive with the correct arguments, not that Postgres resolves a live race, which was out of scope for this unit-test-only pass (see environment note at the top of this document) |

**Full unit suite:** 85 suites / 754 tests passing. `tsc --noEmit` clean. `eslint` clean on every changed file.

**Migrations applied this phase:** `20260819020000_m06_tool_idempotency`, `20260819030000_s07_support_unique_constraints` — both hand-authored (non-interactive shell blocks `prisma migrate dev`) from reviewed `prisma migrate diff` output, both times excluding the same two lines (pgvector/HNSW false-drift `DROP INDEX`, and an unrelated `RenameIndex` belonging to the concurrent session's in-progress credits migration). Both additive-only except S-07's two `@@unique` conversions, which required (and received) pre-migration data verification.

**Kill-critic check for this phase:**
1. Duplicate service created? No — one `ResilientClientBase`, one `ToolIdempotencyService`, both explicitly designed to replace/extend ad hoc per-engine patterns, not add a second mechanism alongside them. M-07/C-10 was explicitly NOT built as a separate rate-limiter (see Phase 1 status note above — resolved as a config detail of C-07).
2. Provider-specific logic in Core? No — `ResilientClientBase`/`ToolIdempotencyService` are pure and provider-agnostic; the global-vs-per-tenant resource-key CHOICE is provider-specific data supplied by each subclass, not logic baked into the base class.
3. Can the external action bypass approval? Unaffected by this phase — Phase 1's gates are untouched.
4. Can it execute twice? **This is what M-06 fixes.** Verified via a real dedup test (Postiz called exactly once across two identical calls).
5. Can a worker crash orphan an external effect? Improved but not eliminated: `ToolIdempotencyService` claims its row before calling the effect (same principle as `publish_now`'s existing pattern), so a crash mid-effect leaves a visibly `PENDING` row rather than a duplicate — an operator can see and reconcile it, matching the existing `publish_now` precedent exactly.
6. Cross-tenant access? S-07 directly closes a real cross-tenant risk (the `chatwootAccountId` webhook-auth lookup key). C-07's per-tenant Chatwoot resource key prevents a NEW cross-tenant availability bug (one tenant's outage tripping another's breaker) that a naive single-global-key design would have introduced.
7. Can an LLM claim success without provider confirmation? Unaffected by this phase.
8. Expensive-loop trigger? No — rate limiting (C-07) if anything reduces this risk for Postiz.
9. Sensitive data retained indefinitely? Not addressed this phase (S-09, still pending).
10. Reusable by a future AI Employee? Yes by design — `ResilientClientBase` and `ToolIdempotencyService` are both engine-agnostic; any future engine client extends the former, any future external-effect tool calls the latter.
11. Weakened an existing safety mechanism? No — `publish_now`'s existing, working idempotency pattern was deliberately left untouched rather than "upgraded" to the new generic mechanism, avoiding unnecessary regression risk to a proven path.
12. Works in both execution modes? Not engine-mode-sensitive — these changes are at the tool-executor/client layer, below both workflow engines equally.
13. Is the test actually executed? Yes — all counts above are from actual `jest` runs this session.
14. Is the deployed code the code verified? Not applicable yet — nothing from this phase has been proposed for deployment.

**Not yet done from the baseline's remaining items:** C-09 (**needs re-scoping against the concurrent session's `CreditsModule`/`ModelCostRate` work — do not build a second budget mechanism without checking what it already covers**), S-09 (retention), M-08 (consent wiring), M-10 (analytics).

---

## Phase 3/4 — C-09 (deferred), Support Retention (S-09), Consent Wiring (M-08), Marketing Analytics (M-10) (COMPLETE)

| Gap | Previous State | Current State | Evidence | Tests | Remaining Risk |
|-----|----------------|---------------|----------|-------|----------------|
| C-09 | Baseline called for a Company→Employee→Execution→Tool/API→LLM cost-control hierarchy extending `UsageService` | **DEFERRED, not implemented.** Investigated `apps/api/src/modules/credits/` (`CreditBalanceService`, `CreditReservationService`, `CreditLedgerService`, `CreditRateAdminService`, `credit-reservation-sweep`), whose own module doc comment states it is mid-build by the concurrent session: "Phase 2 adds the core money-moving primitives... with ZERO real call sites wired yet (Phase 3)." Building a second, competing budget-hierarchy mechanism now would directly violate Rule 2 (never build a second engine the codebase is already building) | N/A — decision only | N/A | Re-open C-09 once `CreditsModule` reaches its own Phase 3 (real call sites wired) and evaluate whether it already covers the Company/Employee/Execution budget hierarchy, or whether a thin extension is still needed on top of it |
| S-09 | `DataRetentionService` swept workflow runs/attempts/outbox/knowledge/memory/conversations/attachments, but had ZERO coverage of `SupportConversation`/`SupportMessage` — Chatwoot support history retained forever regardless of `SecurityPolicy.dataRetentionDays` | Added `sweepSupportConversations(companyId, cutoff, dryRun)` — only conversations with `status:'RESOLVED'` age out (mirrors `WorkflowRun`'s terminal-state-only rule); an `OPEN`/`ESCALATED` conversation is never swept regardless of age. Messages are counted before the cascade delete. No archiving (unlike `WorkflowRun`) — S-09's whole point is real PII erasure, not a queryable snapshot. **Deliberately excluded from any sweep, documented in the code's own comment:** `ChatwootAccount` (connection config, not conversation content) and `MarketingConsent`/`MarketingSuppression` (these are the compliance record itself — deleting evidence of consent/withdrawal on a timer would be the opposite of compliance) | `data-retention.service.ts` (`RetentionCounts`/`ZERO` gained `supportConversations`/`supportMessages`) | New e2e case in `data-retention.e2e-spec.ts`: `'sweeps a RESOLVED support conversation and its messages, but leaves OPEN ones and the ChatwootAccount alone'` — **ran against real Postgres, all 11 tests in the suite passed** | None identified for the mechanism; the exclusion of `ChatwootAccount`/consent records from sweeping is a deliberate compliance decision, not an oversight — stated in-code so a future editor doesn't "complete" the sweep by adding them |
| M-08 | Marketing workflow templates' consent gate trusted a workflow-trigger-supplied `{{trigger.consentVerified}}` boolean — any caller could set this to `true` with no actual consent check, silently bypassing the entire compliance mechanism `SuppressionService`/`MarketingConsent` already implements elsewhere | New no-OAuth `marketing` skill (mirrors `scheduling`'s `connection:{type:'none'}` pattern) with one tool, `check_consent` — normalizes addresses via the existing, reused `extractRecipients` helper, then checks each address against `SuppressionService.isSuppressed` + `latestConsent.status==='GRANTED'`. `mkt.email-campaign` template rewired: the old `CONDITION` reading the trusted boolean replaced with a `checkConsent` TOOL_ACTION followed by a `CONDITION` reading `{{consentCheck.result.allConsented}}` — the real check, not a caller-supplied claim | `catalog.ts` (new `marketing` skill), `real-skill-executor.ts` (`marketingCheckConsent`, executor gained a 10th constructor param, mechanically updated across `skills.module.ts` + 2 e2e spec files + 27 unit-spec call sites), `marketing-workflow-templates.catalog.ts` | `real-skill-executor.spec.ts` new `marketing.check_consent (M-08)` block (4 cases); `workflow-templates.catalog.spec.ts`/`marketing-workflow-templates.catalog.spec.ts` (structural validation, unchanged, still pass); new `marketing-email-campaign-consent.e2e-spec.ts` (3 cases: no consent record → FAILED, GRANTED → COMPLETED, WITHDRAWN → FAILED) — **ran against real Postgres with the executor forced to real/auto mode, all 3 passed for the real reason** (see false-positive note below) | **Caught during verification, not after:** the first e2e run passed the two "blocked" cases for the WRONG reason — ambient `SKILL_EXECUTOR=mock` made `MockSkillExecutor` return a generic sandbox shape lacking `allConsented`, so the CONDITION evaluated false for all three cases including the one that should have proceeded. Fixed by forcing `AutoSkillExecutor`/`RealSkillExecutor` via `overrideProvider(SKILL_EXECUTOR_TOKEN)` in the test's own `beforeAll` — the same documented convention `engines-support.e2e-spec.ts` already established for exactly this failure mode. Re-run confirmed all 3 pass for the real reason |
| M-10 | No marketing analytics existed anywhere — Postiz's own per-post/per-integration metrics were never fetched or stored, so the platform had zero reporting on campaign performance | `PostizClientService` gained `getIntegrationAnalytics`/`getPostAnalytics` (both routed through the existing `postizFetch` helper, so already C-07-resilience-wrapped for free) — response shapes marked `IMPLEMENTED_UNVERIFIED` pending a live Postiz instance. New read-only `postiz.get_post_analytics` tool (NOT `highRisk` — it's a read, not an external mutation) resolves a local `ScheduledPost`→real `postizPostId`, mirroring `get_post_status`'s exact lookup pattern. New `MarketingSyncService.snapshotAnalytics()` — deliberately a SEPARATE method from `sweep()`, not folded into its 10-minute cadence, because doing so would immediately re-exhaust the 90/hr Postiz cap C-07 just fixed; wired to `/admin/cron/marketing-analytics` on a daily cron (`vercel.json`) instead, matching every other worker-only sweep's serverless-cron pattern | `postiz-client.service.ts`, `catalog.ts`, `real-skill-executor.ts` (`postizGetPostAnalytics`), `marketing-sync.service.ts` (`snapshotAnalytics`), `cron.controller.ts`, `vercel.json` | `postiz-client.service.spec.ts` (+3: integration analytics fetch, post analytics fetch, error-propagation); `marketing-sync.service.spec.ts` (+2: writes one snapshot per CONNECTED account, one account's failure doesn't stop the others and is counted separately); `real-skill-executor.spec.ts` (+4: resolves and returns analytics, not-found, not-yet-published guard, client-error surfaced as a failed result not a throw) — all unit, real-provider verification pending a live Postiz instance (stated, not hidden) | Response-shape assumptions (`PostizIntegrationAnalyticsDto`/`PostizPostAnalyticsDto`) are loosely typed and explicitly labeled unverified against a real Postiz instance — should be confirmed and tightened before this is presented as "REAL PROVIDER VERIFIED" per Rule 18 |

**Full unit suite:** 85 suites / 767 tests passing (`npx jest --config ./test/jest-unit.json --maxWorkers=2`). `tsc --noEmit -p tsconfig.json` clean. `eslint` clean on every changed file. Two e2e specs (`data-retention.e2e-spec.ts` S-09 case, `marketing-email-campaign-consent.e2e-spec.ts` M-08, 3 cases) ran against real Postgres this phase; M-10 was verified at the unit level only (Postiz's real analytics endpoint shape has not been confirmed against a live instance).

**Migrations applied this phase:** none — S-09/M-08/M-10 all reused existing tables (`SupportConversation`/`SupportMessage`, `MarketingConsent`/`MarketingSuppression`, `ScheduledPost`) with no schema changes required.

**Kill-critic check for this phase:**
1. Duplicate service created? No — C-09 was explicitly NOT built to avoid competing with the in-progress `CreditsModule`; S-09 extends the existing `DataRetentionService`; M-08's `marketing` skill reuses `extractRecipients`/`SuppressionService` rather than reimplementing consent logic; M-10 reuses `postizFetch` (already resilience-wrapped) and `MarketingSyncService` rather than a new sync engine.
2. Provider-specific logic in Core? No — `SuppressionService`/`DataRetentionService` additions are engine-agnostic; the genuinely Chatwoot/Postiz-specific pieces (retention's Chatwoot table names, analytics' Postiz endpoints) correctly stay inside `engines/support`/`engines/marketing`.
3. Can the external action bypass approval? Unaffected — M-10's new tool is read-only and not `highRisk`; M-08 adds a real check where a bypassable trust existed, closing rather than opening a gap.
4. Can it execute twice? Unaffected this phase (M-06 already covers the mutating marketing/support tools).
5. Can a worker crash orphan an external effect? `snapshotAnalytics()` is fully idempotent by nature (a read + upsert-free create per sweep is naturally safe to re-run; a crash mid-loop just leaves some accounts unsnapshotted until the next daily run, no partial/duplicate state).
6. Cross-tenant access? `sweepSupportConversations` is `companyId`-scoped like every other retention sweep; `snapshotAnalytics` iterates all CONNECTED accounts cross-tenant by design (same pattern as `sweep()`), each snapshot correctly stamped with its own `companyId`.
7. Can an LLM claim success without provider confirmation? M-08 directly fixes exactly this pattern (a trusted boolean standing in for a real check).
8. Expensive-loop trigger? No — `snapshotAnalytics`'s daily cadence was chosen specifically to avoid re-exhausting the Postiz rate cap.
9. Sensitive data retained indefinitely? **This is what S-09 fixes** for Support; Marketing consent/suppression records are deliberately NOT swept (they are the compliance evidence, not PII to erase).
10. Reusable by a future AI Employee? Yes — `ToolIdempotencyService`/`SuppressionService`/`DataRetentionService` patterns are already engine-agnostic; `marketing.check_consent`'s pattern (a read-only compliance-check tool gating a template's CONDITION) is directly reusable for any future outbound-communication engine.
11. Weakened an existing safety mechanism? No.
12. Works in both execution modes? Not engine-mode-sensitive — all four changes are at the retention-sweep/skill-executor/cron layer, below both workflow engines equally.
13. Is the test actually executed? Yes — every number above is from an actual `jest`/e2e run this session, including catching and fixing the M-08 false-positive rather than reporting the first (wrong) green run.
14. Is the deployed code the code verified? Not applicable yet — nothing from this phase has been proposed for deployment.

**Remaining from the original baseline, unstarted:** C-09 (deferred pending `CreditsModule` Phase 3), Phase 6 (operational UI for Handoff/retention/analytics), Phase 7 (full E2E/browser/failure-injection sweep across both engine modes for everything shipped in Phases 1-4).
